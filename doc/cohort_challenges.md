# Cohort Challenges

This document records the cohort challenge materials and submission context for the project.

## Included Materials

- `doc/README.md` — repository submission documentation
- `doc/requirements.txt` — listed Python dependencies for reproducibility
- `doc/submission.zip` — packaged submission artifact

## Submission Notes

- The repository includes all required code, scripts, and packaging assets needed to reproduce the tokenizer workflow.
- The `README.md` at the repository root provides the required summary of dataset, training pipeline, evaluation, reproduction steps, and appendix.
- For final GitHub submission, the repository name should be updated to follow the required `C10-team-name` convention.
